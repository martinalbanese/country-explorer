/**
 * La variabile `BASE_URL` contiene l'URL base dell'API che fornisce i dati sui paesi.
 * Viene letta dal file `.env` tramite `import.meta.env.VITE_BASE_URL`.
 */

export const BASE_URL = import.meta.env.VITE_BASE_URL;
